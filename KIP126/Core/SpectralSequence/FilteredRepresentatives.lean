import KIP126.Def.SpectralSequence.Representatives.Proofs

/-!
Compatibility import for the pre-layout representative module path.

The implementation now lives under `KIP126.Def`; this shim keeps the historical
KIPBase compatibility import valid while the layout integration is staged.
-/
