import KIP126.Def.SpectralSequence.Representatives.Proofs

/-!
# Compatibility import for filtered representatives

The canonical implementation lives in
`KIP126.Def.SpectralSequence.Representatives.Proofs`.  This module preserves the
historical import path used by `KIPBase.Compatibility.FilteredComplex` while the
formalization is migrated to the `Def/` layout.
-/
